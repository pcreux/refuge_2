-if current_member.total_heures_guardien > 0
  #refuge
    %p
      Présence : 
      ="#{current_member.total_heures_guardien.round} h"
      - if current_member.total_heures_guardien.round != current_member.total_heures_facturable_guardien.round
        =" (#{current_member.total_heures_facturable_guardien.round} h facturables)"
      %br
      %small
        depuis le
        =current_member.debut_mois_gardien.strftime("%d/%m/%Y")
